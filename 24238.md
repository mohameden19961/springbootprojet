# Tâches pour 24238

Ton rôle est crucial : tu gères le **Livre Intellectuel (`Book`)** et ses **Exemplaires Physiques (`BookItem`)**. Tu dois aussi mettre en place le verrouillage optimiste (Optimistic Locking) pour éviter les conflits simultanés.

## Étape 1 : Les Entités
Faisons l'entité `BookItem` avec l'annotation `@Version` pour l'optimistic locking.

**Fichier : `src/main/java/com/library/domain/BookItemStatus.java`**
```java
package com.library.domain.enums;

public enum BookItemStatus {
    AVAILABLE, BORROWED, RESERVED, LOST, DAMAGED
}
```

**Fichier : `src/main/java/com/library/domain/BookItem.java`**
```java
package com.library.domain;

import com.library.domain.enums.BookItemStatus;
import jakarta.persistence.*;
import org.hibernate.annotations.SQLRestriction;

@Entity
@Table(name = "book_item")
@SQLRestriction("deleted = false")
public class BookItem extends BaseEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false, unique = true, length = 50)
    private String barcode;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private BookItemStatus status;

    @Version
    @Column(name = "version")
    private Long version; // Optimistic locking : Spring gère l'incrémentation tout seul !

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "book_id")
    private Book book;

    // Getters and Setters
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getBarcode() { return barcode; }
    public void setBarcode(String barcode) { this.barcode = barcode; }
    public BookItemStatus getStatus() { return status; }
    public void setStatus(BookItemStatus status) { this.status = status; }
    public Long getVersion() { return version; }
    public Book getBook() { return book; }
    public void setBook(Book book) { this.book = book; }
}
```

## Étape 2 : Le Repository
**Fichier : `src/main/java/com/library/repository/BookItemRepository.java`**
```java
package com.library.repository;

import com.library.domain.BookItem;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

@Repository
public interface BookItemRepository extends JpaRepository<BookItem, Long> {
    Optional<BookItem> findByBarcode(String barcode);
}
```

## Étape 3 : Controller et DTO pour changer le statut
**Fichier : `src/main/java/com/library/controller/BookItemController.java`**
```java
package com.library.controller;

import com.library.domain.BookItem;
import com.library.domain.enums.BookItemStatus;
import com.library.repository.BookItemRepository;
import org.springframework.http.ResponseEntity;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/book-items")
public class BookItemController {

    private final BookItemRepository repository;

    public BookItemController(BookItemRepository repository) {
        this.repository = repository;
    }

    @PatchMapping("/{barcode}/status")
    @Transactional
    public ResponseEntity<String> updateStatus(@PathVariable String barcode, @RequestParam BookItemStatus status) {
        BookItem item = repository.findByBarcode(barcode)
            .orElseThrow(() -> new RuntimeException("Exemplaire non trouvé"));
            
        item.setStatus(status);
        repository.save(item); // Le @Version va s'incrémenter ici, prévenant les modifications concurrentes !
        return ResponseEntity.ok("Statut mis à jour avec succès.");
    }
}
```
*Tâche : Implémente également la classe `Book` et son Controller complet.*
